plugins {
    alias(libs.plugins.android.application)
    alias(libs.plugins.kotlin.android)
}

def defaultVersionCode = 8
def defaultVersionName = "4.2"
def ciVersionCode = project.findProperty("CI_VERSION_CODE")?.toString()
def ciVersionName = project.findProperty("CI_VERSION_NAME")?.toString()

android {
    namespace 'me.bingyue.fuckbiliads'
    compileSdk 34

    defaultConfig {
        applicationId "me.bingyue.fuckbiliads"
        minSdk 27
        targetSdk 34
        versionCode(ciVersionCode ? ciVersionCode.toInteger() : defaultVersionCode)
        versionName(ciVersionName ?: defaultVersionName)

    }

    buildTypes {
        release {
            minifyEnabled true
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }



    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }
}

dependencies {
    compileOnly(libs.api)
}
